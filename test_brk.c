#include <stdio.h>
#include <dlfcn.h>
#include <unistd.h>
#include <errno.h>
#include <string.h>     

int main()
{
        // int var = 4;
        // const char *str = "TEST";

        // printf("adresse du main: %p\nadress lib shared: %p\nadress lib C: %p\naddresse de la variable locale: %p\nadresse d'une constant str: %p\n", &main, dlopen("libBasics.so", RTLD_LAZY), dlopen("stdlib.h", RTLD_LAZY), &var, &str);
        // return 0;

        return (0);
}
