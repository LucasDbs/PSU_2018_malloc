#if !defined(MALLOC_H)
#define MALLOC_H

#include <unistd.h>

// typedef struct s_block * t_block;

// struct s_block {
//         size_t size;
//         t_block next;
//         t_block prev;
//         int free;
//         void *ptr;
//         char data[1];
// };

#endif // MALLOC_H

